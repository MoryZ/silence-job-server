# 任务调度整体架构说明

本文档用于解释 Silence Job Server 的整体运行方式，重点说明任务如何创建、如何调度、如何执行、如何重试，以及告警通知如何串起来。

## 1. 架构分层

系统可以分成五层：

1. 接口层：负责接收任务创建、编辑、触发、查询等请求。
2. 业务层：负责任务、工作流、重试、通知等领域逻辑。
3. 调度层：负责扫描待触发任务、生成任务实例、分发执行。
4. 执行层：负责根据任务类型调用具体执行器和远程执行节点。
5. 数据层：负责保存 Job、Task、Retry、Notify 等核心数据。

## 2. 任务创建链路

任务创建通常从 Job 进入。用户通过接口提交任务名称、所属组、触发方式、执行器类型、参数、阻塞策略、重试次数等信息，服务层把这些属性组装成 Job 记录写入 `sj_job`。

关键点包括：

1. `triggerType` 决定任务是 CRON 触发还是固定间隔触发。
2. `executorType` 和 `executorInfo` 决定实际由哪种执行器处理。
3. `nextTriggerAt` 是调度器扫描的核心依据。
4. `bucketIndex` 用于分桶和分片扫描，避免所有任务都堆在同一轮扫描里。

## 3. 调度执行链路

调度层会周期性扫描 `sj_job`，找出状态开启且到达触发时间的任务。扫描到任务后，不是直接执行，而是先生成 JobTaskBatch 和 JobTask，再把执行动作注册到时间轮里。

这样设计的好处是：

1. 任务扫描和任务执行解耦。
2. 可以对单次批次做状态追踪。
3. 可以支持并发、分片、广播、MapReduce 等不同执行模型。

执行器会根据任务类型选择不同策略，然后通过 RPC 下发到远程执行节点。执行结果会回写到 JobTask 和 JobTaskBatch。

## 4. 重试与死信链路

当 JobTask 执行失败时，系统会发布失败事件，随后把失败任务转换为 RetryTask，写入 `sj_retry_task`。

之后重试定时器会按 `retryInterval` 触发执行。如果重试成功，状态更新为成功；如果一直失败并且超过 `maxRetryTimes`，任务就会进入 `sj_retry_dead_letter`，等待人工处理或后续补偿。

这个链路的核心不是简单重试，而是把失败、重试、死信三种状态清晰地区分开，方便排查和审计。

## 5. 通知告警链路

任务失败后，系统会异步发送告警事件。监听器先缓冲告警，再批量查询通知配置和接收人，最后调用通知分发器把消息发到邮件、钉钉、企业微信等渠道。

通知配置不是直接绑在字符串字段里，而是通过关联表进行管理，这样可以支持多个 Job、Workflow 或 Retry 场景复用同一套告警配置。

## 6. 核心对象职责

1. `Job`：任务定义本身，决定做什么、怎么触发、由谁执行。
2. `JobTask`：一次具体执行实例，承载运行结果。
3. `JobTaskBatch`：一次调度批次，用于汇总和状态追踪。
4. `Retry` / `RetryTask`：失败后的重试链路。
5. `NotifyConfig` / `NotifyRecipient`：通知和接收人配置。
6. `Workflow` / `WorkflowNode`：编排多个 Job 形成流程。

## 7. 一句话总结

这个系统本质上是一个“配置驱动的分布式任务调度平台”：先定义 Job，再由调度器扫描生成任务实例，通过执行器分发执行，失败后走重试和告警链路，最终形成可追踪、可审计、可扩展的闭环。
