<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.old.silence.job.server.infrastructure.persistence.dao.WorkflowTaskBatchDao">

    <select id="selectWorkflowBatchPageList"
            parameterType="com.old.silence.job.server.dto.WorkflowBatchQueryDO"
            resultType="com.old.silence.job.server.vo.WorkflowBatchResponseDO">
        SELECT batch.*,
               flow.workflow_name
          FROM sj_workflow_task_batch batch
                JOIN sj_workflow flow ON batch.workflow_id = flow.id
        ${ew.customSqlSegment}
    </select>

    <select id="selectWorkflowBatchList"
        resultType="com.old.silence.job.server.vo.WorkflowBatchResponseDO">
        SELECT batch.*,
               flow.workflow_name,
               flow.notify_ids
        FROM sj_workflow_task_batch batch
                 JOIN sj_workflow flow ON batch.workflow_id = flow.id
            ${ew.customSqlSegment}
    </select>

</mapper>
