<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.old.silence.job.server.infrastructure.persistence.dao.RetryDao">

    <insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sj_retry (namespace_id, group_name, scene_name, idempotent_id, biz_no,
                                   executor_name, args_str, ext_attrs, next_trigger_at, task_type, retry_status,
                                   created_date, bucket_index, parent_id, is_deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                 #{item.namespaceId},
                 #{item.groupName},
                 #{item.sceneName},
                 #{item.idempotentId},
                 #{item.bizNo},
                 #{item.executorName},
                 #{item.argsStr},
                 #{item.extAttrs},
                 #{item.nextTriggerAt},
                 #{item.taskType},
                 #{item.retryStatus},
                 #{item.createdDate},
                 #{item.bucketIndex},
                 #{item.parentId},
                 #{item.deleted}
            )
        </foreach>
    </insert>

    <update id="updateBatchNextTriggerAtById" parameterType="java.util.List">
        UPDATE sj_retry rt,
               (
                   <foreach collection="list" item="item" index="index" separator="UNION ALL">
                       SELECT
                           #{item.id}               AS id,
                           #{item.nextTriggerAt}    AS next_trigger_at
                   </foreach>
               ) tt
           SET rt.next_trigger_at = tt.next_trigger_at
         WHERE rt.id = tt.id
    </update>

    <select id="selectRetrySummaryList"
            resultType="com.old.silence.job.server.vo.DashboardRetryResponseDO">
        SELECT namespace_id                                          AS namespaceId,
               group_name                                            AS groupName,
               scene_name                                            AS sceneName,
               SUM(CASE WHEN (retry_status = 0) THEN 1 ELSE 0 END)   AS runningNum,
               SUM(CASE WHEN (retry_status = 1) THEN 1 ELSE 0 END)   AS finishNum,
               SUM(CASE WHEN (retry_status = 2) THEN 1 ELSE 0 END)   AS maxCountNum,
               SUM(CASE WHEN (retry_status = 3) THEN 1 ELSE 0 END)   AS suspendNum
        FROM sj_retry a
                 join (SELECT namespace_id AS namespaceId, group_name AS groupName, scene_name AS sceneName
                       FROM sj_retry_scene_config) b
                      on a.namespace_id = b.namespaceId and a.group_name = b.groupName and a.scene_name = b.sceneName
            ${ew.customSqlSegment}
    </select>

</mapper>
