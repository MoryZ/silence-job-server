# 2026-05-21

## logback.xml 优化
- 在 `<configuration>` 下添加 `NopStatusListener`，屏蔽 logback 自身状态日志刷屏（所有 `|-INFO`/`|-WARN` 行）
- 三处 `RollingFileAppender`（fileInfo/fileWarn/fileError）的 `<layout>` 改为 `<encoder>`（`PatternLayout` → `PatternLayoutEncoder`），消除 logback 1.5 兼容性 WARN
- 确认配置三次重配原因：nacos-client 的 `NacosLogbackConfiguratorAdapterV2` 触发 `LoggerContext.reset()`，Spring Boot 重新 apply 导致

## taskBatchStatus 不更新问题诊断
- 场景：CLUSTER 任务执行成功（taskId=682, taskBatchId=84326），但批次状态仍为 RUNNING(2)
- 添加两处诊断日志：
  1. `JobExecutorResultActor.tryCompleteAndStop()`：记录"尝试完成批次"和"批次完成结果"
  2. `AbstractJobExecutorResultHandler.handleResult()`：当 NOT_COMPLETE 检查不通过时，打印 batch 下所有 task 的 id + status
- `select` 子句补了 `JobTask::getId`，确保日志能输出 task id
- 可能原因：批次下还有其他 JobTask 仍为 RUNNING；或者 updateStatus 的 CAS 条件失败
- 下次重启触发任务后看日志定位
- 第二轮日志：`completed:[false]`，且无"批次未全部完成"日志 → 说明所有 task 都完成了但 CAS 失败
- 加第三处日志：`AbstractJobExecutorResultHandler.updateStatus()` 中打印 `updatedRows` 和 CAS 条件，定位批次状态被谁提前改掉
- 下一轮重启后看 CAS 日志
